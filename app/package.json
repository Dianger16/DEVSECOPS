{
  "name": "devsecops-demo",
  "version": "1.0.0",
  "description": "DevSecOps pipeline demo — SonarQube + OWASP ZAP",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --forceExit",
    "test:ci": "jest --forceExit --ci --coverage --coverageReporters=lcov"
  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}